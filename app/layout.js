import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata = {
  title: "SoftSell | Convert Unused Software Licenses Into Cash",
  description: "SoftSell helps businesses maximize the value of idle software assets through our secure, transparent resale marketplace. Get up to 70% of original value for your unused licenses.",
  keywords: "software resale, sell software licenses, software license marketplace, unused licenses, license valuation",
  authors: [{ name: "SoftSell Team" }],
  icons: {
    icon: "/favicon.ico",
  },
};

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <head>
        <meta name="viewport" content="width=device-width, initial-scale=1.0" />
        <meta name="theme-color" content="#0d9488" />
      </head>
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
